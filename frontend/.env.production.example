# Set these as environment variables in the Vercel project dashboard (do not commit a real .env.production). Use wss:// (not ws://) because the backend is served over HTTPS/TLS in production.
VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_WS_BASE_URL=wss://your-backend.onrender.com
