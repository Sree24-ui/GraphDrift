DATABASE_URL=sqlite:///./graphdrift.db

# Comma-separated list of allowed frontend origins for CORS.
# Leave unset to use local-dev defaults (localhost/127.0.0.1 on ports 5173, 5174, 3000).
# Example production value: ALLOWED_ORIGINS=https://graphdrift.example.com
ALLOWED_ORIGINS=
